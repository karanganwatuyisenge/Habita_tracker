import 'package:flutter/material.dart';

class Language {
  final String name;
  final String languageCode;
  final String countryCode;

  const Language(this.name, this.languageCode, this.countryCode);

  Locale get locale => Locale(languageCode, countryCode);

  static Language fromLocale(Locale locale) {
    switch (locale.languageCode) {
      case 'en':
        return const Language('English', 'en', 'US');
      case 'fr':
        return const Language('Français', 'fr', 'FR');
      case 'rw':
        return const Language('Kinyarwanda', 'rw', 'RW');
      default:
        return const Language('English', 'en', 'US');
    }
  }
}
